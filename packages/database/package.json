{
  "name": "@repo/database",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "postinstall": "prisma generate",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:integration": "vitest run --passWithNoTests tests",
    "prisma:generate": "prisma generate",
    "db:migrate:test": "prisma migrate deploy",
    "db:migrate:deploy": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^6.2.1",
    "@repo/domain": "*"
  },
  "devDependencies": {
    "prisma": "^6.2.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
