# Story 4.3: Saldos de caja y banco por moneda

## Status

Ready for Review

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [code_review, pattern_validation, security_check]
```

## Story

**As a** dueño de cuenta,
**I want** ver mi saldo de efectivo y de cada cuenta bancaria de cada negocio, en su propia moneda,
**so that** sepa cuánta liquidez tengo realmente en cada negocio.

## Acceptance Criteria

1. El sistema mantiene el saldo de caja y de cada cuenta bancaria del negocio activo, en la moneda en la que fue definida cada una (FR20, FR33).
2. Cada compra, venta, gasto o pago de resumen que use una cuenta/caja de ese negocio actualiza su saldo inmediatamente.
3. El usuario puede ver el detalle de saldos del negocio activo agrupado por moneda (base para la consolidación del Epic 5).

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` en core-config.yaml

## Tasks / Subtasks

- [x] Task 1: Server Actions `crearCuentaFinanciera` / `obtenerSaldos` (AC: 1, 3)
  - [x] `crearCuentaFinanciera(negocioId, input: { tipo: 'CAJA'|'BANCO', nombre, monedaId })` en `apps/web/src/actions/cuentas-financieras/`
  - [x] `obtenerSaldos(negocioId): Promise<Result<CuentaFinanciera[], ApiError>>` — la UI agrupa con `agruparSaldosPorMoneda` (packages/domain)
  - [x] Excluye tipo `TARJETA` de esta vista (`where: { tipo: { in: ["CAJA", "BANCO"] } }`) — cubierto por Story 4.2 (`listarTarjetas`) como pasivo, no liquidez
- [x] Task 2: Confirmar actualización inmediata de saldo desde otras stories (AC: 2)
  - [x] Confirmado: Story 2.2/3.1/3.3/4.1/4.2 actualizan `cuentas_financieras.saldo_actual` vía `aplicarMovimientoCuenta`/`aplicarMovimientoTarjeta` (Story 4.2) en la misma transacción — esta story no duplica esa lógica, solo la expone
- [x] Task 3: UI de saldos agrupados por moneda (AC: 1, 3)
  - [x] `apps/web/src/app/(app)/laboral/saldos/page.tsx` — agrupa `CAJA`/`BANCO` por `monedaId`, con formulario de alta
- [~] Task 4: Testing (AC: 1, 2, 3) — **test de dominio completo; integración bloqueada, ver Completion Notes**
  - [ ] Test de integración: saldo de una cuenta financiera del negocio activo refleja inmediatamente el efecto de una compra/venta/gasto recién registrado — **no implementado, mismo bloqueo estructural de stories anteriores**
  - [x] Test de dominio: `agruparSaldosPorMoneda` no mezcla montos de monedas distintas sin convertir (`packages/domain/src/cuenta-financiera.test.ts`, agregado en Story 4.2 porque ambas stories comparten el mismo módulo de dominio)

## Dev Notes

### Previous Story Insights

Esta story consolida lo que Story 2.2, 3.1, 3.3, 4.1 y 4.2 ya hacen sobre `cuentas_financieras.saldo_actual` — no introduce un mecanismo nuevo, expone el resultado agrupado por moneda. Es la base directa de la consolidación multi-moneda de Epic 5 (Story 5.3).

### Data Model — CuentaFinanciera

```typescript
export interface CuentaFinanciera {
  id: string;
  cuentaId: string;
  negocioId: string | null;   // null = Personal
  ambito: "LABORAL" | "PERSONAL";
  tipo: "CAJA" | "BANCO" | "TARJETA";
  nombre: string;
  monedaId: string;
  saldoActual: string;        // Tarjeta: negativo = deuda
  limiteCredito: string | null;
}
```
Modelo unificado de Caja, Cuenta Bancaria y Tarjeta de Crédito — un solo concepto ("medio de pago con saldo/deuda") en vez de tres tablas paralelas.
[Source: architecture/data-models.md#CuentaFinanciera]

### Database Schema — tabla `cuentas_financieras`

```sql
create table cuentas_financieras (
  id uuid primary key default gen_random_uuid(),
  cuenta_id uuid not null references cuentas(id) on delete cascade,
  negocio_id uuid references negocios(id) on delete cascade,
  ambito text not null check (ambito in ('LABORAL','PERSONAL')),
  tipo text not null check (tipo in ('CAJA','BANCO','TARJETA')),
  nombre text not null,
  moneda_id uuid not null references monedas(id),
  saldo_actual numeric(18,4) not null default 0,
  limite_credito numeric(18,4),
  created_at timestamptz not null default now(),
  constraint chk_cta_fin_ambito check ((ambito = 'PERSONAL' and negocio_id is null) or (ambito = 'LABORAL' and negocio_id is not null))
);
alter table cuentas_financieras enable row level security;
create policy cuentas_financieras_isolation on cuentas_financieras
  using (cuenta_id = auth.uid() and (negocio_id is null or negocio_id = nullif(current_setting('app.active_negocio_id', true), '')::uuid or current_setting('app.active_negocio_id', true) = '*'));
```
Cada cuenta financiera tiene un único `moneda_id` fijo — el "saldo por moneda" del AC 3 es simplemente agrupar las filas existentes por ese campo, no una tabla nueva.
[Source: architecture/database-schema.md]

### Ledger vs. saldo denormalizado

`cuentas_financieras.saldo_actual` se actualiza en la misma transacción que cada `INSERT` a `movimientos_cuenta`/`movimientos_tarjeta`, vía `aplicarMovimiento` en `packages/database` (no trigger de Postgres). El ledger es la fuente de verdad auditable; el saldo denormalizado es una proyección para lectura rápida — es lo que esta story lee para AC 1/2.
[Source: architecture/database-schema.md — Notas de diseño transversales al esquema]

### Coding Standards Relevantes

- Dinero como Decimal, nunca `number`.
- Moneda Siempre Acompañada: nunca mostrar `saldoActual` sin su `monedaId`.
[Source: architecture/coding-standards.md#Critical Fullstack Rules]

### File Locations

- `apps/web/src/actions/cuentas-financieras/` (`crearCuentaFinanciera`, `obtenerSaldos`)
[Source: architecture/backend-architecture.md#Server Actions Organization]

### Testing

- Integration test contra Postgres real verificando que el saldo se refleja inmediatamente tras cada tipo de transacción (compra/venta/gasto/pago).
- Domain unit test para la agrupación por moneda sin sumar montos de monedas distintas.
[Source: architecture/testing-strategy.md#Test Organization]

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-08-31 | 0.1 | Initial draft | River |
| 2026-08-31 | 0.2 | Validated GO (9/10) — Status: Draft → Ready | @po |
| 2026-09-01 | 0.3 | Implementación completa. Tabla `cuentas_financieras` compartida con Story 4.2 (creada ahí, ya que ambas documentan el mismo DDL) — esta story solo agrega las Server Actions/UI de CAJA/BANCO, sin migración propia. Status: Ready → Ready for Review | Dex |

## Dev Agent Record

### Agent Model Used

Claude (Dex persona), modo YOLO.

### Debug Log References

- `npm run typecheck` / `npm run lint` / `npm run test` / `npm run build` — mismas corridas que Story 4.2 (implementadas en la misma sesión de trabajo, ver esa story para los números exactos: 98/98 tests OK)
- Sin migración propia: `cuentas_financieras` ya fue creada y aplicada al proyecto Supabase real en Story 4.2

### Completion Notes List

- **Decisión ya documentada en Story 4.2:** el DDL de `cuentas_financieras` es idéntico en los Dev Notes de ambas stories — se creó en 4.2 por orden numérico (primera en necesitarla). Esta story consume la tabla ya existente sin duplicar la migración.
- **Task 1 — completa.** `crearCuentaFinanciera` restringe `tipo` a `CAJA`/`BANCO` vía `crearCuentaFinancieraSchema` (Zod `z.enum(["CAJA","BANCO"])`) — TARJETA no es alcanzable por esta vía, coherente con el AC de excluirla. `obtenerSaldos` filtra `tipo: { in: ["CAJA","BANCO"] }` en la query, doble consistencia con el schema de creación.
- **Task 2 — completa (verificación, no implementación nueva).** Confirmado leyendo el código de Story 2.2/3.1/3.3/4.1 (ya actualizado en Story 4.2 con el wiring retroactivo) que todas pasan por `aplicarMovimientoCuenta`/`aplicarMovimientoTarjeta` dentro de su propia transacción — esta story no agrega ninguna lógica de actualización de saldo, solo la lee.
- **Task 3 — completa.** `SaldosPanel` usa `agruparSaldosPorMoneda` (función pura de `packages/domain`, compartida y testeada en Story 4.2) para agrupar visualmente sin sumar montos de monedas distintas — cumple el AC3 literal ("no mezclar sin convertir").
- **Task 4 — parcial, mismo motivo estructural.** El test de dominio de agrupación por moneda está cubierto (agregado durante Story 4.2 porque ambas comparten el mismo archivo de test del módulo `cuenta-financiera`). El test de integración de actualización inmediata de saldo no se implementó (Story 1.5 no existe, sin infraestructura de Postgres de test) — la funcionalidad que testearía sí existe completa (Story 4.2).
- Story queda en `Ready for Review`, no `Done`: falta el test de integración (bloqueo estructural, no funcional).

### File List

- `apps/web/src/actions/cuentas-financieras/crear-cuenta-financiera.ts` (nuevo)
- `apps/web/src/actions/cuentas-financieras/obtener-saldos.ts` (nuevo)
- `apps/web/src/components/saldos-panel.tsx` (nuevo)
- `apps/web/src/app/(app)/laboral/saldos/page.tsx` (nuevo)
- Ver también el File List de Story 4.2 para los archivos compartidos (`CuentaFinanciera` en `packages/domain`/`packages/database`, migración de la tabla).

## QA Results
