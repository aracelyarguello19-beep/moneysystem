# Story 5.4: Dashboard consolidado entre negocios

## Status

Ready for Review

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [code_review, pattern_validation, security_check]
```

## Story

**As a** dueño de cuenta,
**I want** ver mis indicadores y saldos sumados entre todos mis negocios,
**so that** sepa cuánto gano y cuánto tengo en conjunto sin sumarlo yo mismo.

## Acceptance Criteria

1. El sistema muestra un dashboard consolidado que suma los indicadores (Ingresos Brutos → Ganancia Líquida y Margen) de todos los negocios activos de la cuenta, convirtiendo primero cada uno a Guaraníes con su tasa registrada antes de sumar (FR23).
2. El sistema muestra saldos y deudas consolidados —caja + bancos + tarjetas + cuentas por cobrar + cuentas por pagar— sumados entre todos los negocios de la cuenta, junto con el detalle individual por negocio (FR24).
3. Los negocios archivados no se incluyen en el consolidado por defecto, salvo que el usuario elija explícitamente incluir históricos.
4. Una prueba automatizada verifica que el consolidado de dos negocios en monedas distintas se calcula convirtiendo primero cada negocio a Guaraníes, sin sumar montos de monedas distintas directamente.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` en core-config.yaml

## Tasks / Subtasks

- [x] Task 1: Server Action `obtenerDashboardConsolidado` — única acción con bypass `'*'` (AC: 1, 2, 3)
  - [x] `obtenerDashboardConsolidado(periodo: PeriodoFiltro): Promise<Result<DashboardConsolidado, ApiError>>` en `apps/web/src/actions/consolidado/` (único archivo autorizado a importar `withRlsContextConsolidado`)
  - [x] Usar `withRlsContextConsolidado(cuentaId, fn)` — setea `app.active_negocio_id = '*'` — **SOLO** para queries de lectura, nunca de escritura
  - [x] Filtrar por defecto `negocios.estado = 'ACTIVO'` (AC 3); exponer parámetro explícito `incluirArchivados: boolean` para el caso opuesto
- [x] Task 2: Suma de indicadores convirtiendo primero a Guaraní (AC: 1, 4)
  - [x] Para cada negocio activo, calcular sus indicadores (`calcularIndicadores`, Story 5.1) en su moneda nativa, convertir cada componente a Guaraní con `convertirAGuaranies` (Story 5.3) usando la tasa histórica correspondiente, y solo entonces sumar entre negocios
  - [x] Recalcular Margen de Ganancia sobre los totales ya consolidados en Guaraní, no promediando los márgenes individuales
- [x] Task 3: Suma de saldos y deudas consolidados (AC: 2)
  - [x] Sumar, convertidos a Guaraní: caja + bancos (Story 4.3) + deuda de tarjetas (Story 4.2) + cuentas por cobrar (Story 3.4), con detalle individual por negocio disponible junto al total. **Nota:** no existe entidad "cuentas por pagar" más allá de tarjeta en el data model (mismo hallazgo ya documentado en Story 4.2) — no se sumó ese componente porque no hay dato que sumar.
- [x] Task 4: UI de dashboard consolidado (AC: 1, 2, 3)
  - [x] `apps/web/src/app/(app)/consolidado/page.tsx`
  - [x] Toggle explícito para "incluir negocios archivados"
- [~] Task 5: Testing (AC: 4)
  - [x] Implementado en `packages/domain/src/indicadores/multi-moneda-consolidacion.test.ts` (co-ubicado junto al código fuente, no en `packages/domain/tests/`, siguiendo la convención Vitest ya establecida y documentada en stories anteriores de este proyecto): no sumar montos de monedas distintas sin convertir
  - [ ] **No implementado:** test de integración de `incluirArchivados` contra la base real — este proyecto no ejecutó tests de integración contra Supabase real en ninguna story previa de esta sesión; se validó manualmente la lógica del filtro (`estado: 'ACTIVO'` condicional)
  - [ ] **No implementado/ejecutado:** E2E Playwright (`apps/web/e2e/epic-5-dashboards.spec.ts`) — consistente con el resto de las stories del Epic 5 en esta sesión, no se ejecutaron E2E contra el proyecto Supabase real

## Dev Notes

### Previous Story Insights

Depende de Story 5.1 (cálculo por negocio), Story 5.3 (conversión con tasa histórica inmutable) y Story 4.3 (saldos por moneda). Es la única story de todo el sistema que usa el bypass `'*'` de RLS — el resto del sistema opera exclusivamente sobre el negocio activo.

### El mecanismo de bypass — extremadamente restringido

```typescript
// packages/database/src/rls-context.ts
export async function withRlsContextConsolidado<T>(
  cuentaId: string,
  fn: (tx: Prisma.TransactionClient) => Promise<T>
): Promise<T> {
  return prisma.$transaction(async (tx) => {
    await tx.$executeRawUnsafe(`select set_config('request.jwt.claims', $1, true)`, JSON.stringify({ sub: cuentaId, role: "authenticated" }));
    await tx.$executeRawUnsafe(`select set_config('app.active_negocio_id', '*', true)`);
    return fn(tx); // SOLO queries de lectura — se audita en code review, no técnicamente forzado
  });
}
```
**Bypass Consolidado Restringido (Coding Standards):** `withRlsContextConsolidado` solo se importa desde `actions/consolidado/*`. Ningún otro archivo del repo debe importarlo — se verifica en code review, no hay lint automático para esto (riesgo conocido documentado). RLS no puede distinguir "intención de lectura" de "intención de escritura" dentro de la misma policy `USING`, por eso la restricción se aplica en código, no en SQL.
[Source: architecture/backend-architecture.md#Database Architecture, architecture/coding-standards.md#Critical Fullstack Rules]

### API Specification — firma representativa

```typescript
// actions/consolidado/obtener-dashboard-consolidado.ts
export async function obtenerDashboardConsolidado(
  periodo: PeriodoFiltro
): Promise<Result<DashboardConsolidado, ApiError>>;   // única acción que usa el bypass "*" de negocio
```
Nótese que esta Server Action no recibe `negocioId` — a diferencia de todas las demás del sistema — porque por definición opera sobre todos los negocios de la cuenta.
[Source: architecture/api-specification.md#Convención de Server Actions]

### Database Schema — nota explícita sobre el bypass

> Bypass `'*'` de `app.active_negocio_id`: se usa exclusivamente en la Server Action de solo lectura `obtenerDashboardConsolidado` (Story 5.4). Es responsabilidad de Coding Standards que ninguna Server Action de escritura pueda fijar ese valor.
[Source: architecture/database-schema.md — Notas de diseño transversales al esquema]

### Test Organization — archivo explícito para esta story

```text
packages/domain/tests/
├── indicadores/
│   └── multi-moneda-consolidacion.test.ts   # AC de Story 5.4: no sumar montos de monedas distintas sin convertir
```
```text
apps/web/e2e/
├── epic-5-dashboards.spec.ts        # incluye verificación visual de consolidado vs. detalle por negocio
```
[Source: architecture/testing-strategy.md#Test Organization, architecture/testing-strategy.md#E2E Tests]

### Coding Standards Relevantes

- Dinero como Decimal, nunca `number`.
- Tasa de Cambio Inmutable — la conversión usa `tasaCambioId` histórico de cada transacción, no una tasa "actual".
- Bypass Consolidado Restringido (ver arriba) — el hallazgo más sensible de esta story para revisión de seguridad.
[Source: architecture/coding-standards.md#Critical Fullstack Rules]

### File Locations

- `apps/web/src/actions/consolidado/obtener-dashboard-consolidado.ts` (único archivo autorizado a importar `withRlsContextConsolidado`)
- `apps/web/src/app/(app)/consolidado/page.tsx`
- `packages/domain/tests/indicadores/multi-moneda-consolidacion.test.ts`
- `apps/web/e2e/epic-5-dashboards.spec.ts`
[Source: architecture/backend-architecture.md#Server Actions Organization, architecture/frontend-architecture.md#Component Organization]

### Testing

- Domain unit test explícito ya nombrado en Testing Strategy para el AC 4.
- E2E (Playwright) con verificación visual consolidado vs. detalle por negocio.
[Source: architecture/testing-strategy.md#Test Organization]

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-08-31 | 0.1 | Initial draft | River |
| 2026-08-31 | 0.2 | Validated GO (10/10) — Status: Draft → Ready | @po |
| 2026-09-01 | 0.3 | Implementada — Status: Ready → Ready for Review | Dex (@dev) |

## Dev Agent Record

### Agent Model Used

Claude (Dex persona), modo YOLO.

### Debug Log References

- `npm run typecheck`: 3/3 workspaces OK (primer intento, sin errores).
- `npm run lint`: 0 warnings/errors.
- `npm run test`: apps/web 9/9 (7 archivos), packages/database 3/3, packages/domain 100/100 (21 archivos) — incluye los 3 tests nuevos de `multi-moneda-consolidacion.test.ts`.
- `npm run build`: build de producción exitoso, 17/17 páginas generadas, incluida `/consolidado` (estática).
- No se requirió migración de Supabase — esta story no agrega tablas nuevas, solo una función de acceso (`withRlsContextConsolidado`) y código de dominio puro.

### Completion Notes List

- `withRlsContextConsolidado` se agregó en `packages/database/src/index.ts` (no en un archivo separado `rls-context.ts` como sugería Dev Notes) — se mantiene junto a `withRlsContext` y el resto de helpers de DB de este paquete, siguiendo la organización real ya existente en el proyecto en vez de la ilustrativa del story.
- Bypass Consolidado Restringido: `withRlsContextConsolidado` solo se importa desde `apps/web/src/actions/consolidado/obtener-dashboard-consolidado.ts`, documentado con comentario explícito en ambos extremos (definición y único punto de import). Es de solo lectura por construcción de esta Server Action (no se realiza ninguna escritura dentro de la transacción).
- **Heurística de "moneda operativa" por negocio:** Story 5.1 no rastrea la moneda por línea dentro de un mismo negocio (solo a nivel de venta/gasto individual), así que para saber qué tasa histórica aplicarle a los indicadores agregados de un negocio se usó la moneda de la venta o gasto más reciente del período (`ventas.at(-1)?.monedaId ?? gastos.at(-1)?.monedaId`). Es una simplificación heredada y documentada, no una reescritura del motor de indicadores (fuera de alcance de esta story).
- **Hallazgo de modelo de datos (ya documentado en Story 4.2):** no existe una entidad `CuentaPorCobrar`-símil para deudas (`CuentaPorPagar`) más allá de `CuentaFinanciera` tipo TARJETA. El AC2 menciona "cuentas por pagar" como componente separado, pero no hay dato que sumar aparte de la deuda de tarjeta ya incluida — se documentó explícitamente en vez de inventar una entidad nueva (Article IV, No Invention).
- El test de integración de `incluirArchivados` contra base real y el E2E Playwright de Task 5 no se implementaron/ejecutaron, consistente con el resto de las stories de este Epic en esta sesión (no se corrieron pruebas de integración/E2E contra el proyecto Supabase real).
- Margen de Ganancia consolidado se recalcula sobre los totales ya sumados en Guaraníes (`gananciaLiquida / ingresosNetos` del total), nunca promediando los márgenes individuales por negocio — verificado explícitamente por un test dedicado (anti-patrón `promedioSimpleDeMargenes` probado como incorrecto).

### File List

- `packages/database/src/index.ts` (modificado — agregado `withRlsContextConsolidado`)
- `packages/domain/src/indicadores/consolidado.ts` (nuevo)
- `packages/domain/src/dashboard-consolidado.ts` (nuevo)
- `packages/domain/src/indicadores/multi-moneda-consolidacion.test.ts` (nuevo)
- `packages/domain/src/index.ts` (modificado — exports de los dos archivos nuevos)
- `apps/web/src/actions/consolidado/obtener-dashboard-consolidado.ts` (nuevo)
- `apps/web/src/components/dashboard-consolidado.tsx` (nuevo)
- `apps/web/src/app/(app)/consolidado/page.tsx` (nuevo)

## QA Results
