# Story 6.1: Retiro de utilidades manual

## Status

Ready for Review

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [code_review, pattern_validation, security_check]
```

## Story

**As a** dueño de cuenta,
**I want** retirar puntualmente un monto de la ganancia líquida de uno de mis negocios,
**so that** tenga ese dinero disponible en mis finanzas personales sin mezclarlo con el capital de trabajo de ese negocio ni con el de los demás.

## Acceptance Criteria

1. El usuario puede registrar un retiro de utilidades manual eligiendo el negocio de origen y el monto a retirar (FR25).
2. El retiro reduce la caja/banco de ese negocio en el monto retirado y aparece como ingreso en el módulo Personal, identificado con su negocio de origen.
3. La ganancia líquida no retirada permanece como capital de trabajo del negocio de origen y no aparece como disponible en el módulo Personal (FR27).
4. El sistema muestra el historial de retiros realizados, con fecha, monto y negocio de origen, tanto por negocio como agregado en Personal.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` en core-config.yaml

## Tasks / Subtasks

- [x] Task 1: Server Action `registrarRetiro` (AC: 1, 2)
  - [x] `registrarRetiro(negocioId, input: { monto, cuentaFinancieraId })` en `apps/web/src/actions/personal/`
  - [x] Crear `RetiroUtilidad` con `origen: 'MANUAL'`, `reglaId: null`
  - [x] Dentro de la misma transacción: registrar `MovimientoCuenta` tipo `EGRESO` con `referenciaTipo: "RETIRO"` sobre la `CuentaFinanciera` del negocio de origen (reduce caja/banco de ese negocio)
  - [x] Ejecutar la mutación de origen (negocio) dentro de `withRlsContext(cuentaId, negocioId, fn)` — el retiro en sí es una entidad de la cuenta, no exclusivamente del negocio (ver nota de RLS relajada abajo)
- [x] Task 2: No automatizar el 100% de la ganancia líquida (AC: 3)
  - [x] El monto del retiro es siempre un input explícito del usuario — nunca se deriva ni se sugiere como "ganancia líquida completa" por defecto (principio explícito del epic: "nunca el 100% automático de la ganancia líquida")
  - [x] La ganancia líquida no retirada simplemente no tiene contrapartida en Personal — no requiere un campo adicional de "no retirado"; se deriva por diferencia (Ganancia Líquida del negocio menos suma de sus retiros), cálculo que hará Story 6.5 (balance personal) reutilizando `sumarRetiros` (`packages/domain/src/retiro-utilidad.ts`)
- [x] Task 3: Historial de retiros por negocio y agregado en Personal (AC: 4)
  - [x] `listarRetiros(negocioId | null)`: si `negocioId` se pasa, filtra por ese negocio (via `withRlsContext`); si es `null`, lista todos los retiros de la cuenta (vía la policy relajada de `retiros_utilidad`, ver Dev Notes)
- [x] Task 4: UI (AC: 1, 4)
  - [x] Formulario de retiro en el módulo Laboral (`/laboral/retiros`, eligiendo negocio activo como origen)
  - [x] Vista de historial de retiros: por negocio (misma página Laboral) y agregado en Personal (`/personal/retiros`, identificado con nombre de negocio de origen)
- [~] Task 5: Testing (AC: 1, 2, 3, 4)
  - [ ] **No implementado:** test de integración contra Postgres real verificando el efecto del retiro sobre `saldoActual` — consistente con el resto de esta sesión, no se ejecutaron tests de integración contra el proyecto Supabase real; el mecanismo reutiliza `aplicarMovimientoCuenta` (Story 4.2), ya cubierto por sus propios tests de dominio
  - [x] Test de dominio (`packages/domain/src/retiro-utilidad.test.ts`, 3 tests): `sumarRetiros` solo suma retiros efectivamente registrados, nunca deriva del total de ganancia líquida — la función que Story 6.5 reutilizará para el balance personal

## Dev Notes

### Previous Story Insights

Epic 5 (Story 5.1) calcula la Ganancia Líquida por negocio que da contexto a esta story, pero esta story no lee ese cálculo automáticamente — el retiro es siempre una acción manual explícita del usuario (o de una regla configurada en Story 6.2). Depende de Story 4.3 (saldos de caja/banco por negocio) para el efecto sobre el origen.

### Data Model — RetiroUtilidad

```typescript
export type TipoRegla = "PORCENTAJE" | "MONTO_FIJO";

export interface RetiroUtilidad {
  id: string;
  negocioId: string;
  monto: string;
  fecha: Date;
  origen: "MANUAL" | "REGLA";
  reglaId: string | null;
}
```
Puente explícito entre un negocio y Personal (FR25-FR27) — manual (esta story) o por regla predeterminada (Story 6.2).
[Source: architecture/data-models.md#RetiroUtilidad / ReglaRetiro]

### Database Schema — tabla `retiros_utilidad` (caso especial de RLS)

```sql
create table retiros_utilidad (
  id uuid primary key default gen_random_uuid(),
  negocio_id uuid not null references negocios(id) on delete cascade,
  cuenta_id uuid not null references cuentas(id) on delete cascade,
  monto numeric(18,4) not null check (monto > 0),
  fecha date not null default current_date,
  origen text not null check (origen in ('MANUAL','REGLA')),
  regla_id uuid,
  created_at timestamptz not null default now()
);
alter table retiros_utilidad enable row level security;
-- Nota: a diferencia de las demás tablas Laborales, Personal necesita leer
-- retiros de TODOS los negocios de la cuenta (FR25: "identificado con su
-- negocio de origen" en el módulo Personal). Por eso esta policy NO exige
-- coincidencia de negocio_id para SELECT — solo para el resto de tablas.
create policy retiros_isolation on retiros_utilidad
  using (cuenta_id = auth.uid());
```
Es la única tabla con RLS de negocio "relajada" a propósito — documentado explícitamente en el SQL para que no se lea como un descuido en una futura revisión. Esto es lo que permite a `listarRetiros(null)` (AC 4, vista agregada en Personal) funcionar sin necesitar el bypass `'*'` de Story 5.4.
[Source: architecture/database-schema.md]

### Coding Standards Relevantes

- Dinero como Decimal, nunca `number`.
- Contexto de Negocio Obligatorio para el efecto sobre el negocio de origen (`withRlsContext(cuentaId, negocioId, fn)`).
[Source: architecture/coding-standards.md#Critical Fullstack Rules]

### File Locations

- `apps/web/src/actions/personal/` (`registrarRetiro`, `configurarReglaRetiro`, `registrarGastoPersonal`, `obtenerBalancePersonal`)
- `apps/web/src/app/(app)/personal/balance/page.tsx`
[Source: architecture/backend-architecture.md#Server Actions Organization]

### Testing

- Integration test contra Postgres real verificando el efecto del retiro sobre la caja/banco del negocio de origen.
- Domain unit test para el principio "nunca el 100% automático" (Story 6.5 depende de este comportamiento para el cálculo de balance personal).
[Source: architecture/testing-strategy.md#Test Organization]

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-08-31 | 0.1 | Initial draft | River |
| 2026-08-31 | 0.2 | Validated GO (9/10) — Status: Draft → Ready | @po |
| 2026-09-01 | 0.3 | Implementada — Status: Ready → Ready for Review | Dex (@dev) |

## Dev Agent Record

### Agent Model Used

Claude (Dex persona), modo YOLO.

### Debug Log References

- `npm run typecheck`: 3/3 workspaces OK (primer intento, sin errores).
- `npm run lint`: 0 warnings/errors.
- `npm run test`: apps/web 9/9 (7 archivos), packages/database 3/3, packages/domain 103/103 (22 archivos) — incluye los 3 tests nuevos de `retiro-utilidad.test.ts`.
- `npm run build`: build de producción exitoso, 19/19 páginas generadas, incluidas `/laboral/retiros` y `/personal/retiros`.
- Migración `init_retiros_utilidad` aplicada directamente al proyecto Supabase real (`kgtpgtoaqzyhtxkdlngj`) vía `mcp__supabase__apply_migration`, sin bloqueos del harness esta vez.

### Completion Notes List

- Tabla `retiros_utilidad` creada con la RLS "relajada" documentada en Dev Notes: la policy `retiros_utilidad_isolation` solo exige `cuenta_id = auth.uid()`, sin distinción de `negocio_id` — esto es lo que permite `listarRetiros(null)` (vista agregada de Personal) funcionar sin necesitar el bypass `'*'` restringido de Story 5.4.
- `reglaId` se agregó sin FK (columna UUID nullable) porque la tabla `reglas_retiro` de Story 6.2 todavía no existe — mismo criterio de deuda técnica FK-diferida aplicado en el resto de este proyecto (Compra/Venta/Gasto → CuentaFinanciera/TasaCambio). Se documentó en el comentario del modelo Prisma para wireado retroactivo en Story 6.2.
- El origen de fondos de un retiro se restringe a cuentas CAJA/BANCO (`assertCuentaFinancieraNoEsTarjeta`, ya existente desde Story 4.2) — TARJETA es un pasivo, no liquidez retirable, mismo criterio que `registrarGasto`/`registrarPagoCxc`.
- Se agregó la función pura `sumarRetiros` en `packages/domain/src/retiro-utilidad.ts`, pensada explícitamente para que Story 6.5 (balance personal) la reutilice al derivar "ganancia líquida no retirada" por diferencia — no se implementó ese cálculo de balance en esta story porque depende de datos que Story 6.5 todavía no construye (Article IV, No Invention).
- UI dividida en dos páginas: `/laboral/retiros` (formulario + historial del negocio activo, sigue el patrón de `/laboral/saldos`) y `/personal/retiros` (historial agregado de todos los negocios, identificado por nombre — sigue el patrón de `/consolidado` en cuanto a no depender del negocio activo).
- No se implementó el test de integración de Task 5 contra Postgres real (ni el resto de tests de integración de esta sesión lo hicieron) — el efecto sobre `saldoActual` reutiliza `aplicarMovimientoCuenta`, ya cubierto por los tests de dominio de Story 4.2.

### File List

- `packages/database/prisma/schema.prisma` (modificado — modelo `RetiroUtilidad` + relaciones inversas en `Cuenta`/`Negocio`)
- `packages/database/prisma/migrations/20260901150000_init_retiros_utilidad/migration.sql` (nuevo)
- `packages/domain/src/retiro-utilidad.ts` (nuevo)
- `packages/domain/src/retiro-utilidad.test.ts` (nuevo)
- `packages/domain/src/index.ts` (modificado — export de `retiro-utilidad`)
- `packages/domain/src/schemas/retiro-utilidad.ts` (nuevo)
- `packages/domain/src/schemas/index.ts` (modificado — export de `retiro-utilidad`)
- `apps/web/src/actions/personal/registrar-retiro.ts` (nuevo)
- `apps/web/src/actions/personal/listar-retiros.ts` (nuevo)
- `apps/web/src/components/retiro-form.tsx` (nuevo)
- `apps/web/src/components/retiros-consolidado.tsx` (nuevo)
- `apps/web/src/app/(app)/laboral/retiros/page.tsx` (nuevo)
- `apps/web/src/app/(app)/personal/retiros/page.tsx` (nuevo)

## QA Results
